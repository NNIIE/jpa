spring:
#  datasource:
#    url: jdbc:h2:mem:test
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create
#    properties:
#      hibernate:
##        show_sql: true
#        format_sql: true
# SpringBoot 에서는 기본적으로 메모리 DB로 돌리기 때문에 생략 가능

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace
